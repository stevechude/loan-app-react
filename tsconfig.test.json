{
  "extends": "./tsconfig.app.json",
  "compilerOptions": {
    "jsx": "react-jsx", // Ensure JSX support
    "noEmit": true // Disable output for tests
  },
  "include": [
    "src",
    "**/*.spec.ts",
    "**/*.spec.tsx",
    "**/*.test.ts",
    "**/*.test.tsx"
  ]
}
